// Warehouse Inventory Management
//  A warehouse management system tracks incoming shipments by adding product details to an array named inventoryList.
//  How would you implement a function that appends new products to the array each time a shipment arrives?
let arr = [];
function addProduct(product) {
    arr.push(product);
}
let product={ id: 1,quantity: 10,price: 10};
addProduct(Product);
console.log(Product)
 