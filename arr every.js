//1 Kya sabhi patients admitted hain? 
// const patients = [{ admitted: true }, { admitted: true }]; 
// const allAdmitted = patients.every(p => p.admitted); 
//  console.log(allAdmitted); 

//2 Kya sabhi doctors available hain? 
// const doctors = [{ available: true }, { available: true }]; 
// const allAvailable = doctors.every(d => d.available); 
// console.log(allAvailable);  

//3 Kya sabhi medicines in stock hain? 
// const medicines = [{ stock: 10 }, { stock: 5 }]; 
// const inStock = medicines.every((m) => m.stock > 0); 
// console.log(inStock);  

//4 Kya sabhi reports ready hain? 
// const reports = [{ ready: true }, { ready: true }]; 
// const allReady = reports.every((r) => r.ready); 
// console.log(allReady);  

//5 Kya sabhi test results normal hain? 
// const results = [{ normal: true }, { normal: true }]; 
// const allNormal = results.every(r => r.normal); 
// console.log(allNormal);  

//6 Kya sabhi patients vaccinated hain? 
// const patients = [{ vaccinated: true }, { vaccinated: true }]; 
// const allVaccinated = patients.every(p => p.vaccinated); 
// console.log(allVaccinated);  

//7 Kya sabhi appointments confirmed hain? 
// const appointments = [{ confirmed: true }, { confirmed: true }]; 
// const allConfirmed = appointments.every(a => a.confirmed); 
// console.log(allConfirmed);  

//8 Kya sabhi surgeries scheduled hain? 
// const surgeries = [{ scheduled: true }, { scheduled: true }]; 
// const allScheduled = surgeries.every(s => s.scheduled); 
// console.log(allScheduled);  

//9 Kya sabhi nurses shift par hain? 
// const nurses = [{ onShift: true }, { onShift: true }]; 
// const allOnShift = nurses.every(n => n.onShift); 
// console.log(allOnShift);  

//10 Kya sabhi ICU beds occupied nahi hain? 
// const beds = [{ occupied: false }, { occupied: false }]; 
// const allFree = beds.every(b => !b.occupied); 
// console.log(allFree);  


//                               👔 HR Management 

//11 Kya sabhi employees active hain? 
// const employees = [{ active: true }, { active: true }]; 
// const allActive = employees.every(e => e.active); 
// console.log(allActive);  

//12 Kya sabhi staff ka KYC complete hai? 
// const staff = [{ kyc: true }, { kyc: true }]; 
// const allKYC = staff.every(s => s.kyc); 
// console.log(allKYC);  

//13 Kya sabhi employees ki salary paid hai? 
// const salaries = [{ paid: true }, { paid: true }]; 
// const allPaid = salaries.every(s => s.paid);
// console.log(allPaid);